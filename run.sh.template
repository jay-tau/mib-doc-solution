#!/usr/bin/env bash
set -euo pipefail

input_dir="${1:?usage: ./run.sh <input_pdf_dir> <output_path>}"
output_path="${2:?usage: ./run.sh <input_pdf_dir> <output_path>}"

# Replace this with your pipeline. The command should read every PDF in
# "$input_dir" and write predictions to "$output_path".
python3 solution.py "$input_dir" "$output_path"
